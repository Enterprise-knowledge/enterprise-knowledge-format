interface:
  display_name: "EKF Bootstrap"
  short_description: "Bootstrap, grow, and validate EKF knowledge bases"
  default_prompt: "Use $ekf-bootstrap to create or validate an EKF repository, then turn added source material into governed markdown concepts with provenance."
